

function About({ title, description }) {
  return (
    <div style={{ textAlign: "center", padding: "20px" }}>
      <h2>{title}</h2>
      <p>{description}</p>
     
    </div>
  );
}

export default About;